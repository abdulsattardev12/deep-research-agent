# Legacy Streamlit UI

Optional single-file Streamlit app from the original demo.

```bash
cd legacy
pip install -r requirements.txt
streamlit run deep_research_openai.py
```

Prefer the FastAPI + Next.js app in `backend/` and `frontend/` for the full experience.
